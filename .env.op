# 1Password secret references for `op run --env-file=.env.op`
# These are safe to commit — they are references, not values.

EXPENSIFY_PARTNER_USER_ID=op://work/expensify-api/partner_user_id
EXPENSIFY_PARTNER_USER_SECRET=op://work/expensify-api/partner_user_secret
